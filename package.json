{
  "name": "portfolio-monorepo",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "pnpm@10.12.1",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "preview": "turbo run preview",
    "check": "turbo run check",
    "ingest": "turbo run ingest",
    "web": "pnpm --filter @portfolio/web",
    "rag": "pnpm --filter @portfolio/rag-server"
  },
  "devDependencies": {
    "turbo": "^2.5.6"
  }
}
